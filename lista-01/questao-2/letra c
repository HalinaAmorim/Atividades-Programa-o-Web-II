let n1 = parseFloat(prompt("Nota N1:"));
let n2 = parseFloat(prompt("Nota N2:"));
let media = (n1 * 2 + n2 * 3) / 5;

if (media >= 7)
    console.log(`Média: ${media.toFixed(1)}  Aprovado!`);
else
    console.log(`Média: ${media.toFixed(1)}  Reprovado!`);